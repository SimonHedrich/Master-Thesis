# Docs

> Keep this file up to date whenever new files are added to this directory.

This directory contains documentation, analysis, and notes produced during the Master's Thesis on optimizing deep learning object detection models for real-time inference on embedded hardware.

## Files

### Analysis & Reference

| File | Description |
|------|-------------|
| `2026-03-09_thesis-overview.md` | High-level overview of the thesis subject, research objectives, development paths, and technical constraints — intended as a context prompt for LLMs. |
| `2026-03-09_hardware-proxy-selection.md` | Analysis of SBC alternatives to the Qualcomm QCS605 for development and benchmarking. Recommends the Raspberry Pi 5 (8GB) as the primary proxy device. |
| `2026-03-12_research-and-experimentation-plan.md` | Detailed research and experimentation plan covering dataset selection, model choices, training pipeline (zero-shot → fine-tuning → KD ladder → quantization), evaluation framework, and expected academic contributions. |
| `2026-03-19_species-label-selection.md` | Final analysis and decisions for the student model's 225 output classes: research findings on class count ceilings for nano models, detailed consolidation reasoning by taxonomic group, complete label table, and summary statistics. |
| `2026-03-20_species-label-selection-extended.md` | Extended 480-class label list: minimal pruning of the PO's 483 labels (removes nocturnal/tiny/range-limited species) plus 17 genus/family fallback entries for hierarchical inference. For use with larger student models or two-stage pipeline comparisons. |
| `2026-03-19_species-label-research.md` | Deep research report on optimal taxonomic output design for lightweight mammal detection models, covering class capacity limits, label granularity strategy, North American coverage gaps, and hierarchical fallback architecture. |
| `supplementary-dataset-research-prompt.md` | LLM research prompt for identifying open wildlife image datasets to supplement the GBIF/SpeciesNet training data. Covers commercial license requirements, gap species analysis, and dataset combination strategy. |
| `2026-03-24_supplementary-dataset-research.md` | Research report on commercially viable open wildlife datasets (LILA BC, GBIF, Open Images, COCO), gap coverage analysis, license risk assessment, and data combination strategy. |
| `2026-04-29_dataset-supplementation-plan.md` | Concrete step-by-step plan for building the training dataset: LILA BC download, GBIF gap export, Open Images/COCO integration, label noise handling, synthetic data, and unified dataset assembly with estimated ~160k–300k final images. |

### Progress Notes

| File | Description |
|------|-------------|
| `progress_notes/2026-03-05_first-meeting.md` | Notes from the first supervisor meeting covering quantization, knowledge distillation, transfer learning, and methodology decisions. |
| `progress_notes/2026-03-11_dataset-stakeholder-meeting-and-model-architecture.md` | Meeting with Danielle (AX Visio Product Owner) on dataset preparation, YOLOv5 licensing, geo-filtering, and model architecture research (teacher/student candidates, research question). |
| `progress_notes/2026-03-18_speciesnet-pipeline-and-experiment-design.md` | Analysis of the current Swarovski pipeline (YOLOv5s + SpeciesNet), the two-stage vs. one-shot research gap, model size comparison (~200× gap), dataset status, and KD training strategy. |
| `progress_notes/2026-03-24_dataset-gap-analysis-and-supplementation-strategy.md` | LILA BC filtering results (3.2M images, 115/225 classes covered), 110-class gap analysis by region, and supplementation strategy: iNaturalist Open Data S3, NACTI, Open Images/COCO, Wikimedia Commons, BioCLIP 2 pseudo-labeling. |
| `progress_notes/2026-03-30_wikimedia-category-crawling.md` | Motivation and implementation of the Wikimedia Commons category crawl (`scripts/crawl_wikimedia_categories.py`) and keyword-based filter (`scripts/filter_wikimedia_categories.py`) that produce `reports/wikimedia_categories_filtered/`. |
| `progress_notes/2026-04-24_synthetic-data-kd-experimental-design.md` | Experimental design proposal for the synthetic data + KD thesis contribution: three-tier class stratification (data-rich / parity / synth-only), budget analysis ($300 → 12–15 species × 150 images), synthetic generation strategy, KD-specific hypotheses under data scarcity, and evaluation methodology for small test sets. |
| `progress_notes/2026-04-24_training-setup-and-model-smoke-test.md` | Training environment setup (4 Docker images for YOLO/SpeciesNet/NanoDet/PaddlePaddle), smoke test results for all teacher and student models on RTX 3060, full SpeciesNet two-stage pipeline implementation (MegaDetector + EfficientNetV2-M, soft-label generation for KD), NanoDet-Plus-m and PicoDet-S training configs (nc=225), and class adaptation approach per model family. |
| `progress_notes/2026-04-25_synthetic-scenario-diversity.md` | Problem analysis for repetitive synthetic images (same posture/background per species), Gemini prompt length research (32K chars vs DALL-E 1K / SD 77 tokens), four solution options, and recommendation to pre-generate per-species ecological scenario banks via a cheap text LLM. |
| `progress_notes/2026-06-09_contamination-flagging-and-augmentation-implementation.md` | Orchestrated implementation log for two plans: (1) cross-species contamination flagging in multi-box images (`scripts/dataset_quality/0-dump_speciesnet_labels.py`, `14-flag_multi_animal_contamination.py`, `15-apply_contamination_decisions.py`, FiftyOne `--flagged-review` mode) producing `reports/multi_animal_contamination.*` (11,354 flagged images); (2) data augmentation for the distillation-vs-direct comparison (basic set + Setup-C compositing wired into the YOLOv5s pipeline with seed discipline and a distillation-safety assert). Records the host-only (no-Docker) SpeciesNet-label workaround, per-step subagent instructions/results, a senior-dev bug-fix pass, and the manual steps left for the user. |
| `progress_notes/2026-06-11_evaluation-suite-implementation.md` | Orchestrated implementation log for the standalone model-evaluation suite (`scripts/training/yolov5s/eval_suite/`): predict-once-score-many-ways design, the three-subagent + senior-review orchestration, the frozen data contracts, and the validation (scorer reproduces training-time `evaluate()` to 4 dp). Records the sample-bias trap, the torchmetrics `-1` sentinel bug fix, the genus-rollup + curated-override coarse grouping (`reports/lookalike_groups.csv` via `16-build_lookalike_groups.py`), and the manual override-review step left for the user. |
| `progress_notes/2026-07-13_mlflow-log-table-crash-and-resume.md` | Incident report: the first full YOLOv5s run crashed at epoch 137/200 because per-epoch `mlflow.log_table` calls overflowed the hidden 8000-char `mlflow.loggedArtifacts` run tag (silent truncation → corrupted JSON). Documents the root cause, the fix (plain `log_artifact` + try/except guard, per-epoch `last.pt` checkpointing), and the new `--resume-from` checkpoint-resume support. |
| `progress_notes/2026-07-16_yolov5s-underperformance-hyp-scaling-fix.md` | Root-cause investigation for yolov5s (74 epochs, mAP50=0.371) badly underperforming yolo26n (53 epochs, mAP50=0.614). Disproves the initial anchor-mismatch hypothesis (BPR=1.0 against real training boxes) and identifies the real cause: `yolov5s_model.py` was missing YOLOv5's own nc/nl/imgsz loss-gain autoscaling, under-weighting classification loss ~2.8x for this 225-class dataset. Documents the fix (`_hyp_dict()` autoscaling + a new `autoanchor.py` fit-audit module) and verification. |
| `progress_notes/2026-07-22_yolo26n-eval-oom-crash-diagnosis-and-fix.md` | Diagnoses a yolo26n post-training `--full-eval` that appeared "crashed": training had actually already finished (early-stopped at epoch 179, best.pt @ epoch 158) and the OOM-killer silently killed the subsequent eval step (`journalctl -k`). Root cause: `eval_suite/scoring.py::score()` scored all 225 classes in one `torchmetrics.MeanAveragePrecision` call over the full 75k-image test set. Documents the fix (chunked updates, then a per-class-at-a-time rewrite validated bit-identical against the original on a synthetic reference case, plus a `report.py` fix skipping a redundant Tier 2.1 recompute) and the completed evaluation's results (mAP 0.523 mixed / 0.481 real, ~5 hour runtime — the runtime itself is a follow-on problem tracked in `plans/2026-07-22_eval-suite-scoring-performance-investigation.md`). |
| `2026-05-04_automatic_image_qualitiy_filtering.md` | Design notes and rationale for the automated image quality filtering pipeline: staged funnel (metadata → heuristics → MegaDetector → Florence-2 VLM rescue → Florence-2 caption generation → LLM caption evaluation → SpeciesNet classification + 225-class filtering), implementation details for each script (1–7), key design decisions, and per-source strategy. |
| `2026-04-02_synthetic-image-generation-model-research.md` | Research report on local text-to-image models for generating wildlife training images on an RTX 3060 12 GB: model profiles (FLUX.1, SD 3.5, SDXL family), benchmarked generation speeds, acceleration techniques (NF4, torch.compile, DeepCache, Nunchaku), and a tiered recommendation with sample diffusers code. |
| `2026-04-29_gpu_training_options.md` | Analysis of NVIDIA A40 GPU options for the KD training campaign: VRAM requirements per model, comparison of 48 GB single-instance vs 2 × 12 GB split, and recommendation (two 12 GB instances) with rationale and practical scheduling notes. |

| `2026-06-03_yolov5s-training-pipeline.md` | High-level overview of the custom YOLOv5s fine-tuning pipeline: architecture diagram, module responsibilities, step-by-step process walkthrough, data contract, MLflow logging reference, and key hyperparameters. |

### Plans

Decision records and design documents written before implementation.

| File | Description |
|------|-------------|
| `plans/2026-04-29_pipeline-timeline-and-parallel-work.md` | Timeline and parallel-work coordination for the dataset quality pipeline. |
| `plans/2026-04-30_speciesnet-classification-strategy.md` | Detailed design decisions for pipeline steps 6–7: how to handle SpeciesNet misclassifications (hierarchical match levels), multi-animal images (retain for KD advantage), human co-presence (retain, track with flag), 225-class taxonomy mapping algorithm, and which detections to classify. Basis for `6-classify_speciesnet.py` and `7-filter_speciesnet.py`. |
| `plans/2026-05-04_dataset-construction-strategy.md` | Per-class data strategy: source trust levels (iNaturalist / Wikipedia / GBIF trusted; ImageCV / OpenImages require SpeciesNet filtering), four tiers by post-filter image count (< 100 / 100–499 / 500–1499 / ≥ 1500), synthetic image budget (≤ €500 / max ~10k images), manual review policy, and the Tier 3 real-vs-synthetic comparison design. |
| `plans/2026-05-04_dataset-construction-action-plan.md` | Companion to the construction strategy: evaluates the strategy against `2026-05-04_images-per-class-analysis.md`, overlays track-specific thresholds (KD student 300 / teacher 500 / Track B 1000) on the four tiers, identifies the missing trusted-vs-unverified breakdown in `reports/speciesnet_filter.md`, and lists prioritised next steps and open decisions before dataset assembly. |
| `plans/2026-05-06_dataset-caps-and-synthetic-counts.md` | Final dataset split design: revised 4-band structure (A < 150 / B 150–249 / C 250–399 / D ≥ 400) with 200-image training budgets for Bands A–C, per-class training caps and test splits, synthetic image targets (13,700 images / €685), and real-vs-synthetic comparison experiment for 5 Band D classes. Supersedes the original 4-tier boundaries. |
| `plans/2026-05-06_unreviewed-sn-fail-classes.md` | Follow-on to the SN shrinkage investigation: audits all unreviewed Tier 2/3 classes where `family_mismatch_high_confidence` or `low_speciesnet_confidence` caused >60% image loss. Classifies cases by fail reason, estimates recoverable images, and recommends a two-track resolution (manual review for family_mismatch classes; threshold relaxation for near-threshold match_level classes). |
| `plans/2026-05-12_synthetic-image-generation-strategy.md` | Design document for generating the 12,600 synthetic training images (Band A: 200/class × 50 classes; Band B: 100/class × 26 classes). Covers the six dimensions of image variation (angle, distance, behavior, environment, lighting, occlusion), per-species ecological guild assignments, train/val split rationale (160/40 curated split for Band A; all-train for Band B), the full prompt template with worked examples (walrus, kinkajou), JSONL image-list schema, and pre-generation checklist (scientific name mapping, QC pipeline, bounding-box annotation via MegaDetector). |
| `plans/2026-05-18_synthetic-diversity-improvements.md` | Post-preview analysis and fixes: root cause of walking/camera-facing pose monotony in the first 50 generated images per class, changes to the scene profile generation prompt (explicit resting/foraging/non-frontal requirements per category, guild-specific additions), angle description and prompt template hardening, and the regeneration procedure (clear profile cache, delete all prompt `.txt` files, re-run `1-generate_image_list.py`). |
| `plans/2026-05-18_synthetic-labeling-pipeline.md` | End-to-end plan for producing COCO bounding box annotations for the 12,600 synthetic images: 5-stage pipeline (MegaDetector inference → single-detection triage review → multi-animal bbox labeling → COCO export → FiftyOne verification), per-script design, data formats, routing logic, and implementation order. |
| `plans/2026-05-25_dataset-split-real-image-selection.md` | Plan for assembling the final dataset from quality-passed real images: composite image quality scoring system (bbox area fraction, edge proximity, single-animal preference, MD confidence), per-band train/val/test allocation rules (Bands A–D), source diversity cap for Band D training selection, validation set philosophy (mid-Q stratified sampling), and output formats (split manifest JSON, per-class summary JSON, three COCO exports, methodology report). **Implemented** by `scripts/dataset_quality/12-assign_dataset_splits.py` (2026-05-25): 144k train / 12k val / 63k test images across 225 classes, exported to `data/real/annotations_{train,val,test}.json`. |
| `plans/2026-06-02_yolov5s-training-pipeline.md` | Full implementation plan for the custom YOLOv5s fine-tuning pipeline: module-by-module specification, MLflow logging contract, dataset contract, pre-flight checks, and verification steps. Basis for `scripts/training/yolov5s/`. |
| `plans/2026-06-07_data-augmentation-strategy.md` | Analysis of the comparability-vs-performance tension for data augmentation across the multi-model training campaign. Distinguishes model-generic (safe for cross-model comparison) from model-specific augmentations (mosaic, MixUp). Presents four options (shared conservative set / no-aug baseline / two-tier experiments / ablation follow-on) and recommends a shared HFlip + HSV + scale-crop set for the primary runs, with mosaic reserved for a YOLOv5s-only ablation. |
| `plans/2026-06-08_fix-incomplete-gt-annotations.md` | Root-cause analysis of why every image in the real COCO JSONs has exactly one ground-truth bounding box, despite MegaDetector detecting multiple animals. Identifies the bug in `12-assign_dataset_splits.py` (`detections` array silently dropped in `load_all_passed_images()`), quantifies scope (~15–31 % of images affected per source), and proposes three fixes: pipeline fix (A), post-processing patch script (B), and visualization sampling improvement (C). |
| `plans/2026-06-09_flag-cross-species-contamination-multi-box.md` | Follow-on to the multi-box GT fix: the fix made every confident detection a GT box, but each box inherits the image's single class — so images filed under species X that also contain a different animal Y now carry mislabeled boxes. Plan reuses the already-computed per-box SpeciesNet classifications (script 6) and the taxonomic match-level machinery (script 7's `_compute_match_level`) to auto-flag only boxes that differ from the expected class *beyond family level* (forgiving zebra-species and African/Asian-elephant look-alikes). Specifies a flagging script (14), FiftyOne review workflow, and a decision-apply script (15), with a scale estimate (57k multi-box images; 32k naive upper bound that the tolerance band must collapse). |
| `plans/2026-06-10_model-evaluation-strategy.md` | Strategy for evaluating and comparing trained detectors given two peculiarities (visually near-identical species; heterogeneous per-class training bands + real/synthetic test domains). Names the four evaluation axes (granularity / test domain {mixed/real/synthetic} / training band / COCO metric vector), quantifies the combinatorial explosion, and tames it with a "slices, not the cube" principle (default cell = fine · mixed test · all classes; one-axis perturbations), with the real-only breakout kept as the primary-evaluation + public-comparison anchor. Reframes the detect/coarse/fine granularity ladder as a nested error decomposition (Δ_coarse, Δ_fine) backed by TIDE and a look-alike within-group confusion matrix; recommends a frozen genus-rollup + curated-override label→group table for the coarse level. Defines the band×granularity and real-vs-synthetic domain-shift cross-tabs, public-benchmark comparability rules (lead with class-agnostic mAP_detect), statistical hygiene (test-limited classes, macro vs. weighted, bootstrap CIs), and a 3-tier reporting structure. Implementation reuses `evaluation.py` via category-ID remapping; **implemented** by `scripts/training/yolov5s/eval_suite/` (see `plans/2026-06-11_evaluation-script-implementation.md`). |
| `plans/2026-06-11_lookalike-groups-review.md` | Decision record refining the frozen coarse-grouping table (strategy doc §5 open question #1) after the manual review step. Reviews all 28 v1 multi-member groups against the still-frame confusability criterion; splits two over-merged genera (`panthera` → lion / tiger / rosette-cats; `equus` → zebras / unstriped equids) and merges the true gazelles (Grant's/Thomson's/springbok), all as frozen curated overrides. Records the "considered but not merged" cases (river otters, small spotted cats, cheetah, clouded leopard, foxes). Produces `reports/lookalike_groups_v2.csv` (174 groups, 30 multi-member) via `16-build_lookalike_groups.py`; v1 (`lookalike_groups.csv`) preserved as audit baseline; `eval_suite/grouping.py` repointed to v2. |
| `plans/2026-06-11_evaluation-script-implementation.md` | Implementation plan for the evaluation suite specified by the 2026-06-10 strategy. Defines the modular `eval_suite/` package (predict → score → report), the frozen data contracts (cached COCO predictions JSON; GT index; label-remap dicts), the predict-once-score-many-ways design, and the orchestration (three parallel subagents on `grouping`/`predict`/`scoring`, orchestrator-written `report`/`run_evaluation` + post-training hook + critical review). **Implemented & validated**: scorer reproduces the training-time `evaluate()` to 4 dp; produces Tier 1/2/3 Markdown + CSV/JSON + MLflow. |

### Subdirectory: `synthetic-model-comparison/`

Planning and reference material for a controlled comparison of image-generation models (API and local) for producing synthetic wildlife training data — motivated by the supervisor's requirement that the single-model production choice (Gemini "Nano Banana") be qualitatively evaluated against alternatives rather than chosen by eye. See its own `README.md` for the full index. Contains: `00_motivation-and-research-question.md`, `01_experiment-design.md`, `02_class-selection.md` (the 12 proposed classes — 3 zebras, robust-test anchors, rare species), `03_api-models-landscape-and-pricing.md` (OpenAI/Google/FLUX/Stability/etc. pricing, dimensions, batch, licensing), `04_local-models-and-output-parameters.md` (prompt-length limits, resolutions, throughput), `05_prompt-strategy-and-length-limits.md`, `06_evaluation-methodology.md` (blind rubric + auto proxies + downstream real-test mAP), `07_open-questions-and-what-to-reconsider.md`, `08_classes-and-models-overview.md` (condensed class list + model cost/prompt-length table), `09_test-subset-build.md` (materializing the 12-class real test subset into `data/synthetic_model_comparison/` via `scripts/synthetic_model_comparison/0-build_test_subset.py`), `10_train-subset-incumbent-selection.md` (selecting the incumbent generator's synthetic train subset for 7 of 12 classes by reusing already-generated production images — stratify-by-environment + greedy pose/shot-type/distance/lighting/occlusion diversification to pick 100 of 200 for the 6 rare Bucket-3 classes, take-all for Grévy's zebra — via `scripts/synthetic_model_comparison/1-select_train_subset_incumbent.py`), `11_detector-architecture-selection.md` (deciding the fixed Axis-C detector: YOLO26n over YOLOv5s, per the KD strategy doc's precedent and a log-derived training-time estimate showing time is not a differentiator at this experiment's data scale), and `scraped_sources/` (verbatim OpenAI & Gemini pricing pages).

| `plans/2026-06-24_post-review-filtering-plans.md` | Three post-review plans: applying contamination decisions to `data/real/`, a post-filtering image-count overview table (`17-post_filter_counts.py`), and look-alike group suggestions mined from rejected multi-animal images (`17b-build_lookalike_candidates.py`). |
| `plans/2026-06-30_knowledge-distillation-and-teacher-finetuning-strategy.md` | Strategy for the next training stage: additional lightweight student models plus knowledge distillation using the MegaDetector+SpeciesNet pipeline as teacher. Directly answers three open questions — whether fine-tuning MD+SN makes sense given dataset label provenance (yes; the circularity risk is disclosed as a documented thesis-text limitation rather than addressed by carving out a different test set, so every model stays comparable on one fixed evaluation instrument), whether YOLOv5s is an appropriate KD student (no — it's the size of Swarovski's current production detector; recommends starting with YOLO11n/YOLO26n before NanoDet-Plus-m/PicoDet-S), and why fine-tuning the teacher isn't redundant with using it for KD (offline two-stage KD: fine-tune-then-freeze is the prerequisite phase, not an alternative). Explains why feature-based KD (CWD/FGD/LD) is excluded due to the two-stage-detector-vs-single-stage-detector architecture mismatch, recommends response-based classification KD + MegaDetector secondary-detection pseudo-labels instead, and lays out the Phase 0–4 experimental ladder reusing the existing `eval_suite/`. Supersedes the teacher/model choices (not the general approach) of `2026-03-12_research-and-experimentation-plan.md`. Quantization/QAT explicitly out of scope. |
| `plans/2026-06-30_yolo26-kd-and-teacher-finetune-implementation-plan.md` | Turns the KD strategy doc's ladder into three concrete builds: (A) `scripts/training/yolo26n/`, a direct fine-tune mirroring `yolov5s/`'s data prep/optimizer/scheduler/autostop verbatim (batch size and loss-internal `HYP_*` gains explicitly exempted — different architecture, different VRAM/loss calibration), with the YOLO26-specific engineering risk isolated to two spots: adapting Ultralytics' `model.loss(batch)`/`E2ELoss` (anchor-free, NMS-free, no objectness term, needs a per-epoch `loss_fn.update()` call) and replacing YOLOv5's NMS-based eval decode with YOLO26's already-deduplicated one-to-one head output; (C) `scripts/training/teacher_finetune/`, fine-tuning SpeciesNet's classifier head (`clf.model`, confirmed directly accessible) on the project's 225-class taxonomy, first needing to actually build the Python-3.11 `Dockerfile.speciesnet` environment that an earlier note described but never committed, and proposing grouped/marginal cross-entropy to handle the 47 genus/family-level classes that have no single correct SpeciesNet leaf index; (B) extends package A in place (not a fork) with a KD training mode, with the central risk being that SpeciesNet's one-distribution-per-image soft label has to be blended into YOLO26's per-anchor, task-aligned-assigned `target_scores` rather than bolted on as a global KL term. |
| `plans/2026-07-22_eval-suite-scoring-performance-investigation.md` | Investigation into why the yolo26n `eval_suite.run_evaluation` full run took ~5 hours after the memory-safe per-class rewrite of `scoring.py::score()` (each of 225 per-class `torchmetrics.MeanAveragePrecision` calls is itself substantial work, since predictions are densely spread — median class touches ~29% of images, so per-class stratification trades memory for time). Profiles the real cost driver, benchmarks `pycocotools` (inconclusive at this density), flags `faster-coco-eval` as the most promising untested lead, and **explicitly rejects confidence-threshold filtering as a "speedup"** — validated to materially change reported mAP (0.616 → 0.526), not just runtime. Plan only; no implementation yet. |
